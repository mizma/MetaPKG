# PKGBUILD
pkgname=metapkg-dev-env-tools
pkgver=0.2
pkgrel=1
pkgdesc="Personal development tool essentials"
arch=('any')
url="https://github.com/mizma/MetaPKG/DevENV"
license=('MIT')
depends=(
    bat
    btop
    clang
    ctags
    diffutils
    docker
    docker-buildx
    docker-compose
    eza
    fzf
    git
    git-delta
    jq
    kitty
    neovim
    npm
    python
    python-pip
    python-pipx
    shellcheck-bin
    starship
    stow
    sysz
    tealdeer
    timg
    tmux
    vscodium
    wget
    zsh
    zsh-completions
)

# No source, no build, no files to install
package() {
    true;
}
