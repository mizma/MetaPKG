# PKGBUILD
pkgname=metapkg-games
pkgver=0.3
pkgrel=1
pkgdesc="Game related packages"
arch=('any')
url="https://github.com/mizma/MetaPKG/Games"
license=('MIT')
depends=(
    # Basic game environment
    game-devices-udev
    gamemode
    gamescope
    lutris
    mangohud
    steam
    umu-launcher
    vita3k-git
    wine

    # Tools
    cdemu-client
    cdemu-daemon
    protonplus
    scrcpy
    syncthing

    ## GOverlay for managing vkBasalt and OptiScaler
    goverlay
    mesa-utils
    vkbasalt
    vulkan-tools
    git
    pascube

    # EmuDeck Dependencies
    bash
    flatpak
    fuse2
    git
    jq
    unzip
    zenity
)

# No source, no build, no files to install
package() {
    true;
}
